# eth2-dev

## 依赖版本信息

### 软件版本

| 软件   | 版本信息                                  |
| ------ | ----------------------------------------- |
| abigen | abigen version 1.14.13-stable             |
| go     | go version go1.22.10 linux/amd64          |
| solc   | Version: 0.8.24+commit.e11b9ed9.Linux.g++ |

### 镜像信息

| **组件**          | **镜像版本**                                   |
| ----------------- | ---------------------------------------------- |
| Prysm Beacon Node | gcr.io/prysmaticlabs/prysm/beacon-chain:v5.3.2 |
| Prysm Validator   | gcr.io/prysmaticlabs/prysm/validator:v5.3.2    |
| Prysm Prysmctl    | gcr.io/prysmaticlabs/prysm/cmd/prysmctl:v5.3.2 |
| Geth              | ethereum/client-go:v1.14.13                    |

## 版本信息说明

进入 ETH2.0 之后，共有三个主要组件：

| **组件类型** | **服务名称** | **主要功能**                     |
| ------------ | ------------ | -------------------------------- |
| 执行层       | geth         | 交易执行 / 状态管理 / JSON-RPC   |
| 共识层       | beacon-chain | 区块共识 / 提议与证明 / gRPC API |
| 验证者       | validator    | 出块 / 投票 / 与共识层连接       |

#### 层间关系

```txt
[ Validator ]  ← gRPC →  [ Beacon Chain ]  ← Engine API →  [ Geth ]
```

- **Validator**：负责区块提议与投票。
- **Beacon Chain**：负责共识与区块生成，协调 validator 和 geth。
- **Geth**：负责交易处理和链状态存储，作为执行层提供区块数据支持

#### **版本信息**

| **组件**          | **镜像版本**                                   |
| ----------------- | ---------------------------------------------- |
| Prysm Beacon Node | gcr.io/prysmaticlabs/prysm/beacon-chain:v5.3.2 |
| Prysm Validator   | gcr.io/prysmaticlabs/prysm/validator:v5.3.2    |
| Prysm Prysmctl    | gcr.io/prysmaticlabs/prysm/cmd/prysmctl:v5.3.2 |
| Geth              | ethereum/client-go:v1.14.13                    |

> 为什么这样选择版本：
>
> - **Prysm v5.3.2**：
>   - 支持至 **Deneb** 分叉，并兼容未来的 **Electra** 和 **Fulu** 配置（向后兼容 Capella）。
>   - 新版本修复了早期 v5.x 中的若干共识与同步问题，稳定性与性能优化良好。
>   - 官方推荐的 Deneb 预生产版本，适合测试环境与研究场景使用。
> - **Geth v1.14.13**：
>   - 当前主流的以太坊执行层客户端，官方推荐的生产版本。
>   - 支持**上海（Shanghai）**与**坎昆（Cancun）**分叉，兼容最新 EIPs 和 Engine API。
>
> 整体方案采用 **Prysm + Geth** 组合，具有以下优势：
>
> - 与以太坊主网架构一致，易于对接主流工具与监控系统。
> - 组件稳定成熟，官方支持全面，且文档完善。
> - 支持 Engine API 与 JWT 授权机制，保证执行层与共识层安全通信。

## 执行

确保docker 已经安装，然后在根目录下 运行，对于每一次重启，都一定要先运行`./clean.sh`

```sh
./clean_chain.sh
```

请先阅读单链的启动说明，再阅读多链的启动说明，并尝试启动

整体运行逻辑：
1. 由一个节点运行 `./start_chain.sh -m` 命令，该节点会生成信标链与执行层的创世文件，并启动主节点；
2. 主节点向其他节点分发创世文件，这三个文件分别是：
   1. `execution/genesis.json`：执行层的创世文件；
   2. `execution/genesis.ssz`：执行层的创世文件的
   3. `consensus/config.yml`：共识层的链参数配置文件
3. 其他节点运行 `./start_chain.sh -s` 命令，依赖主节点的创世文件，启动从节点；
   1. 这里在启动前，需要运行`script/check_peer.sh`脚本，获取beacon-chain的初始链接节点信息，并添加到`docker-compose.yml`中；
   2. 再运行`./start_chain.sh -s` 启动从节点；

具体的启动检查，请参考单节点的启动，这里提供了一个测试脚本 `Test/main.go` 可以配合使用。

## 关键参数信息说明

核心依赖三类，4个文件：

- 集群启动文件：
  - `.env`：负责**变量与端口**统一管理，这里都是外部映射端口，内部端口无需更改
  - `docker-compose.yml`：负责**集群整体编排**；

- Prysm配置文件：
  - `consensus/config.yml`：负责**共识层链参数**；
- 创世文件：
  - `execution/genesis.json`：负责**执行层创世配置**。

### 1. 全局环境变量（.env 文件）

| **变量名**          | **说明**                                                 | **示例**    |
| ------------------- | -------------------------------------------------------- | ----------- |
| CHAIN_ID            | 链ID，需一致用于共识层与执行层                           | 32382       |
| FEE_RECIPIENT       | 交易费用接收地址（执行层账户地址，已解锁）               | 0x1234…b524 |
| BEACON_REST_PORT    | Beacon Chain  REST API 服务端口                          | 13500       |
| BEACON_GRPC_PORT    | Beacon Chain gRPC 接口端口，Validator 节点通过此端口连接 | 14000       |
| BEACON_HTTP_PORT    | Beacon Chain HTTP 服务端口                               | 18080       |
| BEACON_METRICS_PORT | Beacon Chain Prometheus 监控端口                         | 16060       |
| BEACON_PPROF_PORT   | Beacon Chain 性能分析接口（pprof）端口                   | 19090       |
| GETH_ENGINE_PORT    | Geth Engine API端口（与共识层通信）                      | 18551       |
| GETH_HTTP_PORT      | Geth JSON-RPC 端口                                       | 18545       |
| GETH_WS_PORT        | Geth WebSocket 端口                                      | 18546       |

### 2. 集群编排文件（docker-compose.yml）

- **服务划分**：
  - create-beacon-chain-genesis：生成信标链与执行层创世文件
  - geth-remove-db：清除 geth 旧数据
  - geth-genesis：初始化 geth 节点
  - beacon-chain：信标链共识节点
  - geth：执行层节点
  - validator：验证者节点
- **端口管理**：依赖.env统一管理
- **网络**：所有服务通过内部网络eth-pos-net互联，外部按需暴露端口

### 3. 共识层配置文件（consensus/config.yml）

| **参数**                  | **说明**                            | **示例**    |
| ------------------------- | ----------------------------------- | ----------- |
| CONFIG_NAME               | 配置名称（interop/minimal/mainnet） | interop     |
| PRESET_BASE               | 基础参数模板                        | interop     |
| GENESIS_FORK_VERSION      | 创世 Fork 版本                      | 0x20000089  |
| ALTAIR_FORK_EPOCH         | Altair 分叉生效 epoch               | 0           |
| BELLATRIX_FORK_EPOCH      | The Merge 生效 epoch                | 0           |
| CAPELLA_FORK_EPOCH        | Capella 分叉生效 epoch              | 0           |
| DENEB_FORK_VERSION        | Deneb 分叉版本号                    | 0x20000093  |
| TERMINAL_TOTAL_DIFFICULTY | Merge 总难度（控制执行层切换）      | 0           |
| SECONDS_PER_SLOT          | 单 Slot 时间（秒）                  | 12          |
| SLOTS_PER_EPOCH           | 每个 epoch 包含的 Slot 数           | 6           |
| DEPOSIT_CONTRACT_ADDRESS  | 存款合约地址（用于 PoS 存款）       | 0x4242…4242 |

### 4.执行层创世文件（execution/genesis.json）

| **参数**       | **说明**                             | **示例**                         |
| -------------- | ------------------------------------ | -------------------------------- |
| config.chainId | 链 ID                                | 32382                            |
| alloc          | 预分配账户（初始 ETH）               | { “0xabc…”: {“balance”: “0x…”} } |
| difficulty     | 创世块难度                           | “0x1”                            |
| gasLimit       | 初始块 gas 上限                      | “0x1fffffffffffff”               |
| config         | 执行层各分叉生效块高度 / EIP 配置    | { “londonBlock”: 0, … }          |
| 其他参数       | 创世块相关参数（nonce、extraData等） | -                                |

### **📌 关键信息说明**

- **Chain ID** 必须统一配置于 .env、config.yml、genesis.json 三处。
- **FEE_RECIPIENT** 需为 geth 预设账户，用于接收交易费与出块奖励。
- **各类端口** 完全模块化，方便多节点部署或远程访问管理。

---

- **启动自 Capella 分叉**：本网络从以太坊 Capella 硬分叉启动，支持提款功能与优化后的共识机制。

- **验证者质押合约已部署**
  - 地址：0x4242424242424242424242424242424242424242
  - 功能：支持通过向合约质押 32 ETH 注册新的验证者
  - 合约源码：[Deposit Contract](https://github.com/ethereum/consensus-specs/blob/dev/solidity_deposit_contract/deposit_contract.sol)
- **默认执行账户**
  - 地址：0x123463a4b065722e99115d6c222f267d9cabb524
  - 功能：
    - 启动时已预置初始 ETH 余额
    - 可用于发送交易、部署智能合约
    - 作为 Prysm 验证者节点设置的 **手续费接收地址**，用于接收出块奖励和交易手续费

## **共识层与执行层的版本信息表**

### **📊 分叉 / Geth / Prysm /solc版本对应表**

| **分叉阶段**       | **Geth 版本**    | **Prysm 版本** | **solc 版本** | **说明**                        |
| ------------------ | ---------------- | -------------- | ------------- | ------------------------------- |
| **Frontier**       | 任意             | 不涉及         | ≤ 0.4.x       | 早期合约开发阶段                |
| **Homestead**      | 任意             | 不涉及         | 0.4.x         |                                 |
| **Byzantium**      | 任意             | 不涉及         | 0.4.x ~ 0.5.x | EVM opcodes 更新                |
| **Constantinople** | 任意             | 不涉及         | 0.5.x         |                                 |
| **Istanbul**       | 任意             | 不涉及         | 0.5.x ~ 0.6.x |                                 |
| **Muir Glacier**   | 任意             | 不涉及         | 0.6.x         |                                 |
| **Berlin**         | 任意             | 不涉及         | 0.6.x ~ 0.7.x |                                 |
| **London**         | ≥ 1.10.x         | 不涉及         | 0.7.x ~ 0.8.x | EIP-1559，引入 basefee          |
| **Shanghai**       | ≥ 1.13.x         | ≥ v3.x         | 0.8.x         | 建议使用 0.8.7+ ，适配 EIP-4895 |
| **Cancun**         | ≥ 1.15.x         | ≥ v5.0.x       | 0.8.20+       | Blob 交易后，合约侧影响较小     |
| **Deneb**          | ≥ 1.15.x（配合） | ≥ v5.0.x       | 0.8.20+       | 主要关注共识层，不影响合约编译  |
| **Electra**        | ≥ 1.15.x（配合） | v5.1.x ~ v6.x  | 0.8.20+       | 未来升级，合约层基本不受影响    |
| **Fulu**           | ≥ 1.16.x（配合） | v6.x（开发中） | 0.8.20+       | 仅限开发阶段                    |

**内容说明：**

- **solc**：合约编译器，与执行层 EVM 版本强相关。建议：
  - **≥ Shanghai** 阶段统一使用 solc >= 0.8.20；
  - 特别依赖 EVM opcode 更新时需关注 solc 版本。
- **geth**：执行层，处理链上状态与交易，solc 编译的字节码通过 Geth 执行。
- **prysm**：共识层，管理验证者状态及区块共识逻辑，与 solc/合约无直接关系，但与 Geth 协作生成区块。



### 📜 具体时间表



#### 📜** 以太坊分叉时间线（重要分叉）**

| **分叉名称**    | **关键事件/EIP**    | **激活方式**   |
| --------------- | ------------------- | -------------- |
| Frontier        | 创世                | block-based    |
| Homestead       | block-based         |                |
| Byzantium       | block-based         |                |
| Constantinople  | block-based         |                |
| Istanbul        | block-based         |                |
| Muir Glacier    | block-based         |                |
| Berlin          | block-based         |                |
| London          | EIP-1559            | block-based    |
| Arrow Glacier   | block-based         |                |
| Gray Glacier    | block-based         |                |
| **Shanghai**    | EIP-4895（提款）    | **time-based** |
| **Cancun**      | EIP-4844（blob tx） | **time-based** |
| Deneb           | 共识层升级          | epoch-based    |
| Electra（未来） | Prysm新分叉         | epoch-based    |
| Fulu（未来）    | Prysm新分叉         | epoch-based    |



#### **📦 Geth 版本支持对比**

| **Geth 版本** | **支持最高分叉** | **重要说明**                            |
| ------------- | ---------------- | --------------------------------------- |
| 1.12.x 及更早 | London           | 仅支持早期分叉                          |
| 1.13.x        | Shanghai         | **首次支持 time-based**（shanghaiTime） |
| 1.14.x        | Shanghai         | **当前适用**                            |
| 1.15.x        | Cancun           | 支持 cancunTime 与 blob tx              |
| 1.16.x        | Cancun+Deneb     | 最新版本，生产用需谨慎                  |



#### **🛡️ Prysm 版本支持对比（信标链）**

| **Prysm 版本**  | **支持最高分叉**   | **备注**                                          |
| --------------- | ------------------ | ------------------------------------------------- |
| v4.x            | Capella            | 常用于生产                                        |
| v5.0.x ~ v5.3.x | Deneb              | 支持 Deneb，增加 Electra、Fulu 配置，但老配置兼容 |
| v6.x（开发中）  | Deneb+Electra+Fulu | 主力开发分支                                      |

Prysm 从 v5.3.x 开始，在内部 **主网配置**中自动引入了 Electra 等未来分叉版本（Electra、Fulu）。